# Copy to `.env` here (or use repo root `.env`). Required for `uvicorn` on the host.
DATABASE_URL=postgresql+psycopg2://waflow:waflow@127.0.0.1:5432/waflow
REDIS_URL=redis://127.0.0.1:6379/0
JWT_SECRET=change-me-use-long-random-string-in-production
